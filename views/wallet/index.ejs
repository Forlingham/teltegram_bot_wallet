<section id="noWallet" style="display: none">
  <div id="pendingAirdropSection" style="display: none;" class="mb-6">
    <div class="bg-surface-container-lowest rounded-lg p-4 shadow-[0px_12px_32px_rgba(44,47,49,0.06)] border border-warning/20">
      <div class="flex items-center gap-3">
        <div class="w-12 h-12 bg-warning/10 rounded-full flex items-center justify-center shrink-0">
          <span class="material-symbols-outlined text-warning" style="font-variation-settings: 'FILL' 1;">mail</span>
        </div>
        <div class="flex-1">
          <p class="text-xs font-bold text-warning uppercase tracking-wider mb-0.5">待入账红包</p>
          <p class="text-xl font-bold font-headline text-on-surface flex items-center gap-1">
            <span id="pendingAirdropAmount">-</span>
            <img src="/img/logo-128x128.png" class="w-5 h-5 object-contain" alt="SCASH" />
          </p>
        </div>
        <div class="text-right">
          <p class="text-[10px] text-on-surface-variant font-medium"><span id="pendingAirdropCount">-</span> 个红包</p>
          <p class="text-[10px] text-on-surface-variant mt-0.5">创建钱包后自动入账</p>
        </div>
      </div>
    </div>
  </div>

  <header class="w-full flex flex-col items-center text-center mb-8">
    <div class="mb-6 relative">
      <div class="absolute -inset-4 bg-primary-container/20 blur-3xl rounded-full"></div>
      <div class="w-20 h-20 relative z-10 bg-surface-container-low rounded-2xl flex items-center justify-center shadow-lg">
        <span class="material-symbols-outlined text-4xl text-primary" style="font-variation-settings: 'FILL' 1;">account_balance_wallet</span>
      </div>
    </div>
    <h1 class="font-headline text-2xl font-extrabold tracking-tight text-on-background mb-3">
      欢迎使用 SCASH 钱包
    </h1>
    <p class="text-on-surface-variant font-medium leading-relaxed max-w-[280px] text-sm">
      创建、导入或绑定钱包后即可开始使用
    </p>
  </header>

  <div class="w-full space-y-4">
    <a class="w-full group flex items-center p-5 bg-surface-container-lowest rounded-lg shadow-[0px_12px_32px_rgba(44,47,49,0.06)] transition-all duration-200 active:scale-[0.98] text-left border border-transparent hover:border-primary/10" href="/wallet/create">
      <div class="primary-gradient w-14 h-14 rounded-full flex items-center justify-center text-on-primary mr-5 shrink-0 shadow-lg shadow-primary/20">
        <span class="material-symbols-outlined text-2xl" style="font-variation-settings: 'FILL' 1;">add_card</span>
      </div>
      <div class="flex-1">
        <h2 class="font-headline text-lg font-bold text-on-surface">创建钱包</h2>
        <p class="text-on-surface-variant text-sm font-medium">生成全新的加密资产账户</p>
      </div>
      <span class="material-symbols-outlined text-on-surface-variant group-hover:translate-x-1 transition-transform">chevron_right</span>
    </a>

    <a class="w-full group flex items-center p-5 bg-surface-container-lowest rounded-lg shadow-[0px_12px_32px_rgba(44,47,49,0.06)] transition-all duration-200 active:scale-[0.98] text-left border border-transparent hover:border-primary/10" href="/wallet/import">
      <div class="bg-surface-container-high w-14 h-14 rounded-full flex items-center justify-center text-primary mr-5 shrink-0">
        <span class="material-symbols-outlined text-2xl">vpn_key</span>
      </div>
      <div class="flex-1">
        <h2 class="font-headline text-lg font-bold text-on-surface">导入钱包</h2>
        <p class="text-on-surface-variant text-sm font-medium">使用助记词或私钥恢复账户</p>
      </div>
      <span class="material-symbols-outlined text-on-surface-variant group-hover:translate-x-1 transition-transform">chevron_right</span>
    </a>

    <a class="w-full group flex items-center p-5 bg-surface-container-lowest rounded-lg shadow-[0px_12px_32px_rgba(44,47,49,0.06)] transition-all duration-200 active:scale-[0.98] text-left border border-transparent hover:border-primary/10" href="/wallet/bind">
      <div class="bg-surface-container-high w-14 h-14 rounded-full flex items-center justify-center text-primary mr-5 shrink-0">
        <span class="material-symbols-outlined text-2xl">link</span>
      </div>
      <div class="flex-1">
        <h2 class="font-headline text-lg font-bold text-on-surface">绑定钱包</h2>
        <p class="text-on-surface-variant text-sm font-medium">绑定观察钱包，只能领取红包，无法发送</p>
      </div>
      <span class="material-symbols-outlined text-on-surface-variant group-hover:translate-x-1 transition-transform">chevron_right</span>
    </a>
  </div>

  <footer class="w-full flex flex-col items-center mt-8 mb-4">
    <p class="text-[11px] text-on-surface-variant/60 text-center leading-relaxed">
      继续操作即表示您同意我们的<br/>
      <span class="text-primary-dim font-bold">服务协议</span> 与 <span class="text-primary-dim font-bold">隐私政策</span>
    </p>
  </footer>
</section>

<section id="walletHome" style="display: none;margin-top: 0;">
  <div id="backupReminder" class="relative overflow-hidden rounded-lg p-5 mb-4 shadow-[0px_12px_32px_rgba(44,47,49,0.06)]" style="display: none; background: linear-gradient(135deg, #9128ad 0%, #e67aff 100%);">
    <div class="absolute -top-8 -right-8 w-32 h-32 bg-white/10 rounded-full blur-2xl"></div>
    <div class="relative z-10 flex items-start gap-4">
      <div class="w-12 h-12 rounded-full bg-white/20 backdrop-blur-md flex items-center justify-center shrink-0">
        <span class="material-symbols-outlined text-white" style="font-variation-settings: 'FILL' 1;">key</span>
      </div>
      <div class="flex-1">
        <h3 class="font-headline text-lg font-bold text-white mb-1">请尽快备份助记词</h3>
        <p class="text-white/80 text-sm leading-relaxed mb-4">你尚未完成助记词备份，请尽快备份防止资产无法恢复。</p>
        <div class="flex gap-3">
          <button id="btnBackupDone" class="px-5 py-2.5 bg-white text-primary rounded-full font-bold text-sm shadow-lg active:scale-[0.98] transition-transform">我已备份</button>
          <a href="/wallet/recover" class="px-5 py-2.5 bg-white/20 backdrop-blur-md text-white rounded-full font-bold text-sm border border-white/30 active:scale-[0.98] transition-transform">去备份</a>
        </div>
      </div>
    </div>
  </div>

  <section class="relative overflow-hidden">
    <div class="absolute -top-12 -right-12 w-48 h-48 bg-primary/10 rounded-full blur-3xl"></div>
    <div class="relative z-10 space-y-1">
      <div id="homeAvatarArea" class="flex justify-center mb-3" style="display: none;">
        <img id="homeAvatar" src="" class="w-12 h-12 rounded-full object-cover border-2 border-primary/20" />
      </div>
      <div class="flex justify-between items-start">
        <p class="text-on-surface-variant font-label text-sm font-medium">总余额</p>
        <button id="btnRefresh" class="w-9 h-9 flex items-center justify-center rounded-full bg-surface-container-low text-on-surface-variant hover:bg-surface-container hover:text-primary active:scale-90 transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed" disabled>
          <span id="refreshIcon" class="material-symbols-outlined text-lg block transition-transform duration-500">refresh</span>
        </button>
      </div>
      <div class="flex flex-col items-start">
        <h1 id="walletTotalBalanceWrap" class="font-headline font-extrabold tracking-tight text-on-surface flex items-baseline gap-2">
          <span id="walletTotalBalance">0.00</span>
          <img src="/img/logo-128x128.png" class="w-4 h-4 object-contain" alt="SCASH" />
        </h1>
        <div class="flex items-center gap-2 mt-1">
          <span id="balanceFiat" class="text-lg text-on-surface-variant font-medium" style="display: none;"></span>
          <span id="priceTag" class="px-2 py-0.5 rounded-full text-xs font-bold" style="display: none;"></span>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-surface-container-lowest rounded-lg p-4 shadow-sm flex items-center justify-between group cursor-pointer hover:bg-surface-container-low transition-colors duration-200 mt-4">
    <div class="flex items-center gap-3 overflow-hidden">
      <div class="flex-shrink-0 w-8 h-8 rounded-full bg-primary/5 flex items-center justify-center text-primary">
        <span class="material-symbols-outlined text-lg">wallet</span>
      </div>
      <div class="overflow-hidden">
        <p class="text-[10px] text-on-surface-variant font-label uppercase tracking-wider mb-0.5">我的钱包地址</p>
        <p id="walletAddress" class="text-[9px] font-mono text-on-surface truncate pr-4">-</p>
      </div>
    </div>
    <div class="flex-shrink-0 flex items-center gap-2">
      <span id="watchOnlyTag" class="text-[10px] px-2 py-0.5 rounded-full font-bold" style="display: none; background: #f4f7f2; border: 1px dashed #c0392b; color: #c0392b;">观察钱包</span>
      <button id="btnCopyAddress" class="text-slate-400 hover:text-primary transition-colors">
        <span class="material-symbols-outlined text-lg">content_copy</span>
      </button>
    </div>
  </section>

  <section class="grid grid-cols-2 gap-4 mt-4">
    <div class="bg-surface-container-low rounded-lg p-4 flex flex-col gap-1">
      <div class="flex items-center gap-1.5 text-on-surface-variant mb-1">
        <span class="material-symbols-outlined text-sm">check_circle</span>
        <span class="text-[11px] font-bold font-headline uppercase tracking-tight">已确认 (链上)</span>
      </div>
      <p id="walletConfirmedBalance" class="text-sm font-bold text-on-surface flex items-center gap-1">
        <span>0.00</span>
        <img src="/img/logo-128x128.png" class="w-4 h-4 object-contain" alt="SCASH" />
      </p>
    </div>
    <div class="bg-surface-container-low rounded-lg p-4 flex flex-col gap-1 border border-primary/5">
      <div class="flex items-center gap-1.5 text-primary mb-1">
        <span class="material-symbols-outlined text-sm">pending</span>
        <span class="text-[11px] font-bold font-headline uppercase tracking-tight">未确认 (内存池)</span>
      </div>
      <p id="walletUnconfirmedBalance" class="text-sm font-bold text-primary flex items-center gap-1">
        <span>0.00</span>
        <img src="/img/logo-128x128.png" class="w-4 h-4 object-contain" alt="SCASH" />
      </p>
    </div>
  </section>

  <nav class="grid grid-cols-4 gap-3 mt-6">
    <a class="flex flex-col items-center gap-2 group" href="/wallet/send">
      <div class="w-14 h-14 rounded-full bg-gradient-to-br from-primary to-primary-container flex items-center justify-center text-white shadow-lg shadow-primary/20 group-active:scale-90 transition-all duration-300">
        <span class="material-symbols-outlined text-2xl">send</span>
      </div>
      <span class="text-xs font-bold text-on-surface">发送</span>
    </a>
    <a class="flex flex-col items-center gap-2 group" href="/wallet/receive">
      <div class="w-14 h-14 rounded-full bg-surface-container-lowest flex items-center justify-center text-primary shadow-sm group-active:scale-90 transition-all duration-300">
        <span class="material-symbols-outlined text-2xl">download</span>
      </div>
      <span class="text-xs font-bold text-on-surface">接收</span>
    </a>
    <a class="flex flex-col items-center gap-2 group" href="/wallet/inscribe">
      <div class="w-14 h-14 rounded-full bg-surface-container-lowest flex items-center justify-center text-primary shadow-sm group-active:scale-90 transition-all duration-300">
        <span class="material-symbols-outlined text-2xl">edit_note</span>
      </div>
      <span class="text-xs font-bold text-on-surface">刻字上链</span>
    </a>
    <a id="scanQrHomeBtn" class="flex flex-col items-center gap-2 group cursor-pointer">
      <div class="w-14 h-14 rounded-full bg-surface-container-lowest flex items-center justify-center text-primary shadow-sm group-active:scale-90 transition-all duration-300">
        <span class="material-symbols-outlined text-2xl">qr_code_scanner</span>
      </div>
      <span class="text-xs font-bold text-on-surface">扫码</span>
    </a>
  </nav>

  <div id="priceChartCard" class="mt-4">
    <div class="flex justify-between items-end mb-2">
      <div>
        <h2 class="text-base font-headline font-bold text-on-surface">SCASH 7日走势</h2>
        <p id="chartPriceTag" class="text-xs text-on-surface-variant font-medium">市场价格：<span id="chartCurrentPrice">$0.000</span></p>
      </div>
      <div class="text-right">
        <span id="chartPriceChange" class="font-bold font-headline text-lg"></span>
      </div>
    </div>
    <div class="w-full relative overflow-hidden bg-surface-container-lowest rounded-lg border border-outline-variant/10">
      <div class="absolute inset-0 opacity-10 flex items-center justify-center pointer-events-none">
        <div class="w-full h-[1px] bg-outline-variant absolute top-1/4"></div>
        <div class="w-full h-[1px] bg-outline-variant absolute top-2/4"></div>
        <div class="w-full h-[1px] bg-outline-variant absolute top-3/4"></div>
      </div>
      <div id="priceChartContainer" style="width:100%; height:180px;"></div>
      <div id="chartCrosshairPrice" class="absolute top-2 right-2 text-[10px] text-on-surface-variant"></div>
    </div>
  </div>
</section>

<script src="https://unpkg.com/lightweight-charts@4.1.3/dist/lightweight-charts.standalone.production.js"></script>

<script>
  function satoshiToScashString(sats) {
    var sign = sats < 0n ? '-' : '';
    var abs = sats < 0n ? -sats : sats;
    var whole = abs / 100000000n;
    var frac = (abs % 100000000n).toString().padStart(8, '0');
    return sign + whole.toString() + '.' + frac;
  }

  function formatBalanceSmallDecimal(sats) {
    var str = satoshiToScashString(sats);
    var parts = str.split('.');
    if (parts.length === 2) {
      return '<span class="text-4xl font-bold">' + parts[0] + '</span><span class="text-lg font-bold">' + '.' + parts[1] + '</span>';
    }
    return '<span class="text-4xl font-bold">' + str + '</span>';
  }

  function formatBalanceInline(sats) {
    var str = satoshiToScashString(sats);
    return '<span class="font-bold">' + str + '</span>';
  }

  async function loadHome() {
    try {
      var home = await window.SCASH.request('/api/wallet/home');
      if (!home.hasWallet) {
        document.getElementById('noWallet').style.display = 'block';
        document.getElementById('walletHome').style.display = 'none';
        
        if (home.pendingAirdrop) {
          document.getElementById('pendingAirdropAmount').textContent = home.pendingAirdrop.amount;
          document.getElementById('pendingAirdropCount').textContent = home.pendingAirdrop.count;
          document.getElementById('pendingAirdropSection').style.display = 'block';
        }
        
        return;
      }
      document.getElementById('noWallet').style.display = 'none';
      document.getElementById('walletHome').style.display = 'block';
      homeReady = true;
      document.getElementById('walletAddress').textContent = home.address || '-';
      
      if (home.avatarUrl) {
        document.getElementById('homeAvatar').src = home.avatarUrl;
        document.getElementById('homeAvatarArea').style.display = 'block';
      } else {
        document.getElementById('homeAvatarArea').style.display = 'none';
      }
      document.getElementById('backupReminder').style.display = home.showBackupReminder ? 'block' : 'none';
      
      if (home.isWatchOnly) {
        document.getElementById('watchOnlyTag').style.display = 'inline-block';
      }
      
      loadBalance();
    } catch (e) {
      alert('初始化失败：' + e.message);
    }
  }

  async function loadBalance() {
    var btn = document.getElementById('btnRefresh');
    var icon = document.getElementById('refreshIcon');
    btn.disabled = true;
    icon.classList.add('spin-anim');
    icon.classList.remove('text-on-surface-variant');
    icon.classList.add('text-primary');
    
    try {
      var bal = await window.SCASH.request('/api/wallet/balance?includeUnconfirmed=true');
      var utxos = bal.utxos || [];
      var confirmedSats = 0n;
      var unconfirmedSats = 0n;
      
      for (var i = 0; i < utxos.length; i++) {
        var u = utxos[i];
        var uSat = BigInt(Math.round(parseFloat(u.amount) * 1e8));
        if (u.isUnconfirmed) {
          unconfirmedSats += uSat;
        } else {
          confirmedSats += uSat;
        }
      }
      
      var totalSats = confirmedSats + unconfirmedSats;
      cachedTotalSats = totalSats;

      document.getElementById('walletTotalBalance').innerHTML = formatBalanceSmallDecimal(totalSats);
      document.getElementById('walletConfirmedBalance').innerHTML = formatBalanceInline(confirmedSats) + ' <img src="/img/logo-128x128.png" class="w-4 h-4 object-contain" alt="SCASH" />';
      document.getElementById('walletUnconfirmedBalance').innerHTML = formatBalanceInline(unconfirmedSats) + ' <img src="/img/logo-128x128.png" class="w-4 h-4 object-contain" alt="SCASH" />';
      
      updatePriceDisplay();
    } catch (e) {
      document.getElementById('walletTotalBalance').innerHTML = '<span class="text-2xl font-bold">获取失败</span>';
      document.getElementById('walletConfirmedBalance').innerHTML = '<span class="font-bold">-</span>';
      document.getElementById('walletUnconfirmedBalance').innerHTML = '<span class="font-bold">-</span>';
    } finally {
      var icon = document.getElementById('refreshIcon');
      btn.disabled = false;
      icon.classList.remove('spin-anim');
      icon.classList.remove('text-primary');
      icon.classList.add('text-on-surface-variant');
    }
  }

  document.getElementById('btnRefresh').addEventListener('click', function () {
    loadBalance();
  });

  document.getElementById('btnBackupDone').addEventListener('click', async function () {
    try {
      await window.SCASH.request('/api/wallet/backup/complete', {
        method: 'POST',
        body: JSON.stringify({ backedUp: true }),
      });
      document.getElementById('backupReminder').style.display = 'none';
    } catch (e) {
      alert(e.message);
    }
  });

  var cachedTotalSats = null;
  var homeReady = false;
  var lwChart = null;
  var lwSeries = null;

  function updatePriceDisplay() {
    var pd = window.SCASH.priceData;
    if (!pd || !pd.price) return;
    if (!homeReady) return;
    var priceUsd = parseFloat(pd.price.price || 0);
    if (!priceUsd) return;

    // 价格标签
    var priceTag = document.getElementById('priceTag');
    var pct24 = parseFloat(pd.price.changePercent24h || 0);
    var pctSign = pct24 >= 0 ? '+' : '';
    var pctColor = pct24 >= 0 ? '#2f7a42' : '#c0392b';
    priceTag.innerHTML = '$' + priceUsd.toFixed(3) + ' <span style="color:' + pctColor + ';">' + pctSign + pct24.toFixed(2) + '%</span>';
    priceTag.style.display = 'block';

    // 余额法币估值
    if (cachedTotalSats !== null) {
      var balScash = Number(cachedTotalSats) / 1e8;
      var fiatVal = balScash * priceUsd;
      var fiatEl = document.getElementById('balanceFiat');
      fiatEl.textContent = '\u2248 $' + fiatVal.toFixed(2) + ' USD';
      fiatEl.style.display = 'block';
    }

    // 图表右上角当前价格
    document.getElementById('chartCurrentPrice').textContent = '$' + priceUsd.toFixed(3);

    // 7日走势图
    var chartData = pd.priceChart;
    if (chartData && chartData.length > 1 && typeof LightweightCharts !== 'undefined') {
      // 去重：每30分钟只保留第一个数据点
      var seen = {};
      var deduped = [];
      for (var ci = 0; ci < chartData.length; ci++) {
        var ts = chartData[ci].timestamp;
        var key = ts.slice(0, 16);
        if (!seen[key]) {
          seen[key] = true;
          deduped.push(chartData[ci]);
        }
      }

      var pct7 = parseFloat(pd.price.changePercent7d || 0);
      var sign7 = pct7 >= 0 ? '+' : '';
      var color7 = pct7 >= 0 ? '#2f7a42' : '#c0392b';
      document.getElementById('chartPriceChange').innerHTML = '<span style="color:' + color7 + ';"> ' + sign7 + pct7.toFixed(2) + '%</span>';

      document.getElementById('priceChartCard').style.display = 'block';
      requestAnimationFrame(function () {
        renderLWChart(deduped, priceUsd);
      });
    }
  }

  function renderLWChart(data, currentPrice) {
    var container = document.getElementById('priceChartContainer');
    if (!container) return;
    var cw = container.clientWidth;
    if (cw <= 0) return;

    // 清理旧图表
    if (lwChart) {
      lwChart.remove();
      lwChart = null;
      lwSeries = null;
    }

    var isUp = parseFloat(data[data.length - 1].price) >= parseFloat(data[0].price);
    var lineColor = isUp ? '#2f7a42' : '#c0392b';
    var topColor = isUp ? 'rgba(47,122,66,0.28)' : 'rgba(192,57,43,0.20)';
    var bottomColor = isUp ? 'rgba(47,122,66,0.02)' : 'rgba(192,57,43,0.02)';

    lwChart = LightweightCharts.createChart(container, {
      width: cw,
      height: 200,
      layout: {
        background: { type: 'solid', color: 'transparent' },
        textColor: '#999',
        fontSize: 10,
      },
      grid: {
        vertLines: { visible: false },
        horzLines: { color: '#f0f0f0', style: 2 },
      },
      crosshair: {
        mode: LightweightCharts.CrosshairMode.Magnet,
        vertLine: { width: 1, color: 'rgba(47,122,66,0.3)', style: 0, labelVisible: false },
        horzLine: { visible: false, labelVisible: false },
      },
      timeScale: {
        borderVisible: false,
        timeVisible: true,
        secondsVisible: false,
        fixLeftEdge: true,
        fixRightEdge: true,
      },
      rightPriceScale: {
        borderVisible: false,
        scaleMargins: { top: 0.1, bottom: 0.1 },
      },
      handleScroll: { vertTouchDrag: false },
      handleScale: { axisPressedMouseMove: false, pinch: false, mouseWheel: false },
    });

    lwSeries = lwChart.addAreaSeries({
      lineColor: lineColor,
      topColor: topColor,
      bottomColor: bottomColor,
      lineWidth: 2,
      crosshairMarkerVisible: true,
      crosshairMarkerRadius: 4,
      crosshairMarkerBorderColor: '#fff',
      crosshairMarkerBackgroundColor: lineColor,
      priceFormat: { type: 'price', precision: 3, minMove: 0.001 },
    });

    var seriesData = data.map(function (d) {
      return {
        time: Math.floor(new Date(d.timestamp).getTime() / 1000),
        value: parseFloat(d.price),
      };
    });
    lwSeries.setData(seriesData);
    lwChart.timeScale().fitContent();

    // Crosshair tooltip
    var crosshairEl = document.getElementById('chartCrosshairPrice');
    var currentPriceEl = document.getElementById('chartCurrentPrice');
    lwChart.subscribeCrosshairMove(function (param) {
      if (!param || !param.time || !param.seriesData || !param.seriesData.size) {
        crosshairEl.textContent = '';
        currentPriceEl.textContent = '$' + currentPrice.toFixed(3);
        return;
      }
      var val = param.seriesData.get(lwSeries);
      if (val && typeof val.value === 'number') {
        var t = new Date(param.time * 1000);
        var timeStr = (t.getMonth() + 1) + '/' + t.getDate() + ' ' + String(t.getHours()).padStart(2, '0') + ':' + String(t.getMinutes()).padStart(2, '0');
        currentPriceEl.textContent = '$' + val.value.toFixed(3);
        crosshairEl.textContent = timeStr;
      }
    });

    // 监听容器大小变化
    if (window.ResizeObserver) {
      new ResizeObserver(function () {
        if (lwChart) {
          lwChart.applyOptions({ width: container.clientWidth });
        }
      }).observe(container);
    }
  }

  loadHome();

  // 价格加载
  if (window.SCASH.priceLoading) {
    window.SCASH.priceLoading.then(function () { updatePriceDisplay(); });
  }

  /* QR scan from homepage */
  var tg = window.SCASH && window.SCASH.TG;
  if (tg) {
    tg.onEvent('qrTextReceived', function (obj) {
      tg.closeScanQrPopup();
      var raw = (obj.data || '').trim();
      var address = raw;
      /* strip scash: prefix if present */
      if (address.indexOf('scash:') === 0) {
        address = address.slice(6);
      }
      var bech32Prefix = (window.SCASH && window.SCASH.NETWORK && window.SCASH.NETWORK.bech32) || 'bcrt';
      if (address.indexOf(bech32Prefix + '1') === 0) {
        location.href = '/wallet/send?address=' + encodeURIComponent(address);
      } else {
        tg.showAlert('不支持的地址格式');
      }
    });

    document.getElementById('scanQrHomeBtn').addEventListener('click', function () {
      tg.showScanQrPopup({ text: '扫描收款地址二维码' });
    });
  }

  document.getElementById('btnCopyAddress').addEventListener('click', function () {
    var addr = document.getElementById('walletAddress').textContent;
    if (!addr || addr === '-') return;
    navigator.clipboard.writeText(addr).then(function () {
      var btn = document.getElementById('btnCopyAddress');
      var icon = btn.querySelector('.material-symbols-outlined');
      icon.textContent = 'check';
      btn.classList.remove('text-slate-400');
      btn.classList.add('text-primary');
      setTimeout(function () {
        icon.textContent = 'content_copy';
        btn.classList.remove('text-primary');
        btn.classList.add('text-slate-400');
      }, 1500);
    });
  });
</script>